## Where Winds Meet – инструменты локализации

**Софт создан и обсуждается на форуме**: `https://forum.zoneofgames.ru/topic/80635-where-winds-meet/`  
**Автор ключевых изменений**: `suncrash66`

### Назначение этой версии

Эта версия `WWM_Extractor_Files_and_Texts.py` – это базовый набор утилит для работы с ресурсами игры **Where Winds Meet**.  

### Основные функции

- **`extract_file`**  
  Читает бинарный архив игры, распаковывает сжатые блоки и сохраняет каждый блок в виде отдельного `.dat` файла.

- **`pak_file`**  
  Собирает набор `.dat` файлов обратно в единый бинарный архив, используя ZSTD‑сжатие.

- **`extract_text`**  
  - Работает по папке с `.dat` файлами.  
  - Выявляет файлы с сигнатурой текстового блока.  
  - Извлекает строки и сохраняет их в `TextExtractor.csv` в формате:
    - `Number;File;All Blocks;Work Blocks;Current Block;Unknown;ID;OriginalText`
  - Переводы строк `\n` и `\r` сохраняются в виде `\\n` и `\\r`, чтобы структура CSV не ломалась.

- **`pak_text`**  
  - Читает отредактированный `TextExtractor.csv`.  
  - Собирает на его основе новые `.dat` файлы, подставляя изменённые значения `OriginalText` по их `ID`.  
  - Остальная структура бинарных файлов (блоки, офсеты) восстанавливается так, чтобы игра корректно читала новые данные.

### Интерфейс

Скрипт использует **PyQt5 GUI** и даёт четыре основные группы операций:

- **📦 Распаковка файлов** – выбор входного архива и папки для `.dat`.  
- **📦 Запаковка файлов** – выбор папки с `.dat` и сборка нового архива.  
- **📦 Распаковка текста** – выбор папки с `.dat` и создание `TextExtractor.csv`.  
- **📦 Запаковка текста** – выбор `TextExtractor.csv` и папки для новых `.dat` с уже внедрённым переводом.

### Запуск

1. Установить зависимости (минимум: `pyzstd`, `PyQt5` для Python 3.x).  
2. Запустить:

```bash
python WWM_Extractor_Files_and_Texts.py
```