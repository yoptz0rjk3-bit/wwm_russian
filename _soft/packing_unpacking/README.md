## Where Winds Meet – инструменты локализации (GUI)

> Доступна 2 версия `WWM_Extractor_Files_and_Texts_2.py`

**Софт для распаковки/запаковки ресурсов и работы с текстами игры _Where Winds Meet_.**

Основной файл взят с: [zoneofgames](https://forum.zoneofgames.ru/topic/80635-where-winds-meet/)

За основу взял вариант от suncrash66 ❤️

---

### Возможности

- **Работа с игровыми архивами**

  - **Распаковка файлов**: извлечение бинарного архива игры в набор `.dat` файлов.
  - **Запаковка файлов**: сборка набора `.dat` обратно в один игровой архив с использованием ZSTD‑сжатия.
- **Работа с текстами**

  - **Распаковка текста**:
    - По папке с `.dat` файлами создаётся общий CSV `TextExtractor.csv`.
    - Формат строк:
      - `Number;File;All Blocks;Work Blocks;Current Block;Unknown;ID;OriginalText`
    - Переводы строк внутри текста сохраняются как `\\n` и `\\r`, чтобы не ломать CSV.
  - **Запаковка текста**:
    - Читает отредактированный `TextExtractor.csv`.
    - Подставляет новые значения `OriginalText` по их `ID` и собирает обновлённые `.dat` файлы, пригодные для игры.
- **Перевод по ID (удобная работа переводчика)**

  - **Создание файла перевода**:
    - Кнопка **«Создать CSV: ID,OriginalText»** берёт `TextExtractor.csv` и создаёт `translation.csv` (или другой выбранный файл).
    - В результирующий CSV попадают только строки, где `OriginalText` не пустой.
    - Формат:
      `ID;OriginalText`
  - **Применение перевода из CSV**:
    - Принимает CSV‑файл с колонками `ID;OriginalText` (ваш переведённый текст).
    - Создаёт новый `TextExtractor_translated.csv`, где `OriginalText` заменён по совпадающим `ID`.
    - Структура строк (колонки, порядок, служебные поля) остаётся без изменений.
  - **Debug‑файл с тегами**:
    - Кнопка **«Создать debug TextExtractor.csv (теги)»** создаёт копию `TextExtractor.csv`.
    - Для каждой непустой строки `OriginalText` в начале текста добавляется уникальный тег вида `[aB39]`
      (4 символа из `A‑Z`, `a‑z`, `1‑9`).
    - Пустые строки не трогаются, остальная структура CSV полностью сохраняется.
- **Сохранение путей в `config.ini`**

  - Все выбранные в GUI пути (входные/выходные папки и файлы) автоматически сохраняются в `config.ini`
    рядом с `WWM_Extractor_Files_and_Texts.py`.
  - При следующем запуске программы последние использованные пути подхватываются автоматически,
    так что не нужно каждый раз заново всё «протыкивать».

---

### Требования

- **Python** 3.x
- Библиотеки Python:
  - `pyzstd`
  - `PyQt5`

Установка (простейший вариант):

```bash
pip install pyzstd PyQt5
```

---

### Запуск

Находясь в папке с `WWM_Extractor_Files_and_Texts.py`, выполните:

```bash
python WWM_Extractor_Files_and_Texts.py
```

Откроется окно с группами:

- **📦 Распаковка файлов** – работа с игровыми архивами → `.dat`.
- **📦 Запаковка файлов** – сборка `.dat` → архив.
- **📦 Распаковка текста** – `.dat` → `TextExtractor.csv`.
- **📦 Запаковка текста** – `TextExtractor.csv` → обновлённые `.dat`.
- **📑 Перевод по ID** – создание `translation.csv`, применение перевода и генерация debug‑версии `TextExtractor.csv` с тегами.

Интерфейс интуитивный: последовательно выбираете файл/папку и нажимаете нужную кнопку. Все действия и ошибки отображаются в лог‑поле внизу окна.
